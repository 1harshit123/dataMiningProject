[project]
name = "dataminingproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "asyncpraw>=7.8.1",
    "python-decouple>=3.8",
    "rich>=13.9.4",
]

[tool.ruff]
select = ["E", "F", "I"]

[tool.ruff.isort]
known-first-party = []
known-third-party = []
